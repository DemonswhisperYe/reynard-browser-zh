# 简体中文本地化成果记录

## 项目

项目源码目录：

```text
reynard-browser/
```

本次工作目标：为 Reynard Browser 添加第一批简体中文（`zh-Hans`）本地化支持。

## 已完成内容

### 1. Xcode 工程语言配置

已修改：

```text
browser/Reynard.xcodeproj/project.pbxproj
```

在 `knownRegions` 中新增：

```text
zh-Hans,
```

表示工程开始声明支持简体中文区域。

### 2. 新增简体中文资源文件

已新增：

```text
browser/Reynard/Resources/zh-Hans.lproj/Localizable.strings
browser/Reynard/Resources/zh-Hans.lproj/InfoPlist.strings
```

`Localizable.strings` 用于普通 UI 文案翻译。

`InfoPlist.strings` 用于 App 名称、Info.plist 中可见字段翻译。

当前首批翻译包括：

```text
Settings -> 设置
Bookmarks -> 书签
History -> 历史记录
Downloads -> 下载
Add-ons -> 扩展
Browsing -> 浏览
Search -> 搜索
Appearance -> 外观
Compatibility -> 兼容性
Site Permissions -> 站点权限
OK -> 确定
Cancel -> 取消
Remove -> 移除
Allow -> 允许
Ask -> 询问
Deny -> 拒绝
Clear -> 清除
Private Browsing -> 隐私浏览
Upload -> 上传
Confirm Upload -> 确认上传
Add -> 添加
```

`InfoPlist.strings` 当前内容包括：

```text
CFBundleDisplayName -> Reynard
UISceneConfigurationName -> 默认配置
UTTypeDescription -> Firefox 扩展包
```

其中 `Reynard` 作为品牌名保持不翻译。

### 3. 新增源码本地化入口

已新增：

```text
browser/Reynard/Client/Localization/Localization.swift
```

内容为：

```swift
import Foundation

func L(_ key: String, comment: String = "") -> String {
    NSLocalizedString(key, comment: comment)
}
```

主 App 中的 UIKit 文案可以通过 `L("...")` 统一走本地化表。

### 4. 已替换的主要源码位置

#### 主资料库入口

```text
browser/Reynard/Client/Interface/Views/Library/LibraryViewController.swift
```

已本地化：

```text
Bookmarks
History
Downloads
Settings
```

#### 设置首页

```text
browser/Reynard/Client/Interface/Views/Library/Settings/SettingsView.swift
```

已本地化：

```text
Settings
Add-ons
Browsing
Search
Appearance
Compatibility
Site Permissions
OK
```

#### 设置子页面

```text
browser/Reynard/Client/Interface/Views/Library/Settings/Sections/General/Add-ons.swift
browser/Reynard/Client/Interface/Views/Library/Settings/Sections/General/Browsing.swift
browser/Reynard/Client/Interface/Views/Library/Settings/Sections/General/Search.swift
browser/Reynard/Client/Interface/Views/Library/Settings/Sections/General/Appearance.swift
browser/Reynard/Client/Interface/Views/Library/Settings/Sections/General/Compatibility.swift
```

已本地化：

```text
Add-ons
Browsing
Search
Appearance
Compatibility
Settings
Remove
Cancel
```

#### 站点权限

```text
browser/Reynard/Client/Interface/Views/Library/Settings/Sections/Privacy/SitePermissions.swift
browser/Reynard/Client/Interface/Views/Modals/SiteSettings.swift
```

已本地化：

```text
Site Permissions
Ask
Allow
Deny
Clear
```

#### 书签、下载、标签页概览

```text
browser/Reynard/Client/Interface/Views/Library/BookmarksManager/BookmarksManagerView.swift
browser/Reynard/Client/Interface/Views/Library/DownloadsManager/DownloadsManagerView.swift
browser/Reynard/Client/Interface/Views/TabOverview/TabOverviewCollection.swift
```

已本地化：

```text
Bookmarks
Cancel
Private Browsing
```

注意：文件系统路径中的目录名没有翻译，例如：

```swift
appendingPathComponent("Downloads", isDirectory: true)
```

这类字符串属于路径/目录名，不是 UI 文案。

#### Gecko prompt / 文件选择 / 选择菜单

```text
browser/GeckoView/Delegates/PromptDelegate/PromptDelegate.swift
browser/GeckoView/Delegates/PromptDelegate/PromptElements/FilePicker.swift
browser/GeckoView/Delegates/PromptDelegate/PromptElements/SelectionMenu.swift
```

这些文件可能属于独立 target，所以使用 `NSLocalizedString(...)`，没有依赖主 App 的 `L(...)` helper。

已本地化：

```text
OK
Cancel
Upload
Confirm Upload
```

#### 插件弹窗按钮

```text
browser/Reynard/Client/Interface/Views/Modals/AddonPrompts.swift
```

已本地化：

```text
Add
Allow
```

## 已完成检查

已完成以下检查：

1. 确认 `project.pbxproj` 中存在 `zh-Hans`。
2. 确认新增资源文件存在：
   - `browser/Reynard/Resources/zh-Hans.lproj/Localizable.strings`
   - `browser/Reynard/Resources/zh-Hans.lproj/InfoPlist.strings`
3. 确认新增本地化入口存在：
   - `browser/Reynard/Client/Localization/Localization.swift`
4. 检查没有出现错误的双重包装：
   - 没有 `L(L(...))`
   - 没有 `NSLocalizedString(NSLocalizedString(...))`
5. 使用 Python 对 `.strings` 文件做了基础格式检查，检查通过。

## 当前环境限制

当前工作区运行环境是 Linux，不是 macOS，因此无法直接执行 iOS IPA 构建。

项目打包脚本依赖 macOS/Xcode 工具链：

```text
xcodebuild
xcrun
plutil
ldid
```

当前环境缺少这些工具，所以不能在这里直接生成 IPA。

本次实际检查结果：

```text
未发现 *.app
未发现 *.xcarchive
未发现 *.ipa
未发现 *.tipa
未发现 xcodebuild
未发现 xcrun
未发现 Gecko iOS 构建输出 obj-aarch64-apple-ios/dist
```

因此当前环境没有可重新封装的已编译 App，也没有从源码编译 iOS App 所需的 Apple 工具链。不能生成可安装测试的真实 IPA。

## 后续打包方式

项目自带打包脚本：

```text
tools/release/build-app.sh
tools/release/create-ipa.sh
```

在 macOS/Xcode 环境中可尝试：

```bash
cd reynard-browser
tools/release/build-app.sh
tools/release/create-ipa.sh
```

理论产物路径：

```text
dist/Reynard.ipa
dist/Reynard-TrollStore.tipa
dist/Reynard-Jailbroken.ipa
```

## 后续注意事项

1. 当前源码是 ZIP 包解压出来的，不是完整 git clone 仓库。`tools/release/build-app.sh` 中使用了：

```bash
git -C "$ROOT_DIR" rev-parse --short HEAD
```

如果在非 git 仓库中打包，该命令会失败。后续可选择：

- 在 macOS 上重新 `git clone` 项目后应用本次修改。
- 或修改打包脚本，让没有 git 信息时使用 `UNKNOWN` 或时间戳作为 build 号。

2. 项目依赖 Gecko engine 构建产物：

```text
browser/Configuration/Reynard.xcconfig
GECKO_DIST = $(SRCROOT)/../engine/firefox/obj-aarch64-apple-ios/dist
```

如果该目录不存在，Xcode 构建会失败。需要先准备或构建 Gecko engine。

3. 本次只完成第一批高频 UI 文案本地化，不代表项目中所有英文长文本都已翻译。后续可以继续处理：

```text
插件说明
JIT 说明
更新说明
下载/书签更多操作菜单
站点权限长提示
错误提示和状态提示
复数/数量相关文案
```

## 重封装测试包记录

用户提供了官方发布包：

```text
https://github.com/minh-ton/reynard-browser/releases/download/0.4.0/Reynard-TrollStore.tipa
```

已下载到当前工作区：

```text
Reynard-TrollStore.tipa
```

已备份原始文件：

```text
Reynard-TrollStore.original.tipa
```

已解包并向 App bundle 注入简体中文资源：

```text
Payload/Reynard.app/zh-Hans.lproj/InfoPlist.strings
Payload/Reynard.app/zh-Hans.lproj/Localizable.strings
```

已重新封装生成：

```text
Reynard-TrollStore-zh-Hans.tipa
Reynard-TrollStore-zh-Hans.ipa
```

验证结果：

```text
unzip -tq Reynard-TrollStore-zh-Hans.tipa 通过
unzip -tq Reynard-TrollStore-zh-Hans.ipa 通过
包内确认存在 Payload/Reynard.app/zh-Hans.lproj/
```

注意：本次是基于现有 TrollStore 包进行资源注入和重新封装，没有重新编译二进制，也没有重新签名。只有原包中已经通过系统本地化机制读取资源的内容，才可能显示为中文；原二进制中硬编码且未走本地化表的英文不会仅靠注入资源自动变中文。

## 结论

本次已经完成 Reynard Browser 的第一批简体中文本地化落地：

- 工程声明支持 `zh-Hans`。
- 新增简体中文 `.lproj` 资源。
- 新增统一本地化 helper。
- 替换主导航、设置、权限、书签、下载、隐私浏览、Gecko prompt、插件弹窗中的关键英文 UI 文案。

后续需要在 macOS/Xcode 环境中进行构建和真机/模拟器验证。
